/**
 * SVG Fingerprinting Constants
 *
 * CSS styles for SVG measurement.
 */

/**
 * CSS styles for SVG container and emoji text elements.
 */
export const SVG_CONTAINER_STYLES = `
#svg-container {
  position: absolute;
  left: -9999px;
  height: auto;
}
#svg-container .shift-svg {
  transform: scale(1.000999) !important;
}
`;
